/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'images.unsplash.com',
        pathname: '/**',
      },
      {
        protocol: 'https',
        hostname: 'dl.kraken.io',
        pathname: '/**',
      },
    ],
  },

  eslint: {
    // ✅ Prevent ESLint errors from failing `next build` in Docker
    ignoreDuringBuilds: true,
  },
};

module.exports = nextConfig;
