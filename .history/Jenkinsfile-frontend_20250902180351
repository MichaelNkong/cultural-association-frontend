pipeline {
    agent any
    tools {
        git 'Default'
    }
    
    environment {
        RENDER_API_KEY = "rnd_khRDdc5JDqJyZh1p1D7hQmaqfqkx"
        FRONTEND_DEPLOY_HOOK = "https://api.render.com/deploy/srv-cvbc77dds78s73aipv5g?key=thxv2GR6NiQ"
    }

    stages {
        stage('Checkout Code') {
            steps {
                script {
                    checkout([
                        $class: 'GitSCM',
                        branches: [[name: '*/faet']],
                        userRemoteConfigs: [[
                            url: 'https://github.com/MichaelNkong/cultural-association-frontend.git',
                            credentialsId: 'github-credentials'
                        ]]
                    ])
                }
            }
        }

        stage('Run Tests') {
            steps {
                script {
                    // Optional: Add any tests here
                    echo "Running tests..."
                    // sh "npm install && npm test"
                }
            }
        }

        stage('Trigger Render Deployment') {
            steps {
                script {
                    echo "Triggering Render deployment from GitHub..."
                    sh "curl -X POST ${FRONTEND_DEPLOY_HOOK}"
                }
            }
        }
    }
    
    post {
        success {
            echo 'Deployment triggered successfully!'
        }
        failure {
            echo 'Pipeline failed!'
        }
    }
}